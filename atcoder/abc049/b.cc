#include <iostream>
#include <string>

using namespace std;

int main(void) {
  int H, W;
  cin >> H >> W;
  for (int i = 0; i < H; ++i) {
    string C;
    cin >> C;
    cout << C << endl;
    cout << C << endl;
  }
  return 0;
}
